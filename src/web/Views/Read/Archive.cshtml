@inject ISwanService _swanService
@model List<ReadItem>
@{
    Layout = "_LayoutPublic";
}

@foreach(var item in Model)
{
    <h3>@item.BookName</h3>
    <p>@item.CreatedAt.ToCnDate()</p>

}

@section Aside{
    <div class="card">
        <div class="card-header">
            Archive Index
        </div>
        <ul class="list-group list-group-flush">
            @foreach (var item in (await _swanService.GetReadItemsAsync()).GroupBy(x => x.CreatedAt.Year))
            {
                <li class="list-group-item list-group-action">
                    <a href="/read/@item.Key">@item.Key</a>
                </li>
            }
        </ul>
    </div>
}